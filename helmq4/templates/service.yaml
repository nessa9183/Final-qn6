apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmq4.fullname" . }}
  labels:
    {{- include "helmq4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmq4.selectorLabels" . | nindent 4 }}
